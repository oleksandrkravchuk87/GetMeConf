syntax = "proto3";

package api;

service ConfigService{
    rpc SearchConfig(ConfigInfo) returns (ConfigInfo);
    rpc GetConfig(ConfigInfo) returns (Config);
}

message ConfigInfo{
    string ConfigId = 1;
    string ConfigPath = 2;
}

message Config{
    bytes Config = 1;
}