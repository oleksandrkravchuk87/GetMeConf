syntax = "proto3";

package api;

service SearchConfig{
    rpc Request(ConfigInfo) returns (ConfigInfo);
}

service GetConfig{
    rpc Request(ConfigInfo) returns (Config);
}

message ConfigInfo{
    string ConfigId = 1;
    string ConfigPath = 2;
}

message Config{
    bytes Config = 1;
}